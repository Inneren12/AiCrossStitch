<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <application
      android:name="com.appforcross.editor.App"
      android:label="@string/app_name"
      android:theme="@style/Theme.AppForCross">
      <!-- Import Activity (интеграционный экран) -->
      <activity
      android:name="com.appforcross.editor.ui.import.ImportActivity"
      android:exported="false" />
      <!-- (п.2) FileProvider для безопасного шаринга ZIP через content:// -->
     <provider
        android:name="androidx.core.content.FileProvider"
        android:authorities="${applicationId}.fileprovider"
        android:exported="false"
        android:grantUriPermissions="true">
        <meta-data
        android:name="android.support.FILE_PROVIDER_PATHS"
        android:resource="@xml/file_paths" />
     </provider>
    <activity android:name="com.appforcross.editor.dev.DevMenuActivity" android:exported="false"
        android:label="Developer Options"/>
    <activity
        android:name="com.appforcross.app.MainActivity"
        android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>
  </application>

</manifest>
